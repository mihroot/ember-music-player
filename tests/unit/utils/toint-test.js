import toint from 'ember-music-player/utils/toint';
import { module, test } from 'qunit';

module('Unit | Utility | toint');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toint();
  assert.ok(result);
});
