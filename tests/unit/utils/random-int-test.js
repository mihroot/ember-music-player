import randomInt from 'ember-music-player/utils/random-int';
import { module, test } from 'qunit';

module('Unit | Utility | random int');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = randomInt();
  assert.ok(result);
});
